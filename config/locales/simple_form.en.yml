en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: 'Some errors were found, please take a look:'
    # Labels and hints examples
    labels:
      entry_date: 'Admission Date:'
      exit_date: 'Date of discharge:'
      birth_date: 'Birthday:'
      leave_days: 'Leave days:'
      age: 'Age:'
      years: 'Years'
      days: 'Days'
      adm_weight: 'Admission weight:'
      sex: 'Sex:'
      sex_male: 'male'
      sex_female: 'female'
      sex_unknown: 'unknown'
      adm: 'Admission mode:'
      sep: 'Separation mode:'
      los: 'Length of stay:'
      sdf: '!SDF!:'
      hmv: 'Artificial respiration:'
      pdx: 'Primary diagnoses:'
      diagnoses: 'Secondary diagnoses:'
      procedures: 'Procedures:'
      group: 'Group It'
      
      
      # XXX:javax.faces.validator.LongRangeValidator.MAXIMUM=Value is greater than allowable maximum of {0}. 
      # XXX:javax.faces.validator.LongRangeValidator.MINIMUM=Value is less than allowable minimum of {0}. 
      # XXX:javax.faces.validator.LongRangeValidator.NOT_IN_RANGE=Specified attribute is not between the expected values of {0} and {1}. 
      # XXX:javax.faces.validator.LongRangeValidator.TYPE=Value is not of the correct type. 
      # XXX:invalidCode=Invalid Code.
      # XXX:javax.faces.component.UIInput.REQUIRED=Value is required. 
      # adm03=in- and out-patient
      # adm11=Transfered (Length of stay more than 24 hours in the transfering hospital)
      # adm06=Transfered (Length of stay less than 24 hours in the transfering hospital)
      # adm00=not transfered
      # adm99=unknown
      # sep99=unknown
      # sep04=terminated against medical advice
      # sep06=transfered to another hospital
      # sep07=deceased
      # sep00=normal
      # lblDrgName=Name
      # lblHours=Hours
      # btnClear=Clear form
      # lblStay=Stay
      # lblPatient=Patient data
      # #label reason for grouping case as 'ungroupable'
      # grpReason=reason
      # #missing or bad pdx
      # grpBadPDX=primary diagnosis (pdx) missing or bad pdx
      # #not a valid primary diagnosis
      # grpNoPDX=not a pdx
      # #fallen through grouping
      # grpFallThrough=data not matching criteria to fall into a DRG
      # grpBadAge=bad age
      # grpBadSex=bad sex
      # grpBadAdmissionOrLeave=bad reason for admission or leave
      # #babies only
      # grpBadAdmWeight=bad admission weight
      # #bad length of stay
      # grpBadLOS=bad length of stay
      # grpBadSameday=bad sameday value
      # toolTipLoc=Seitenlokalisation (R, L, B)
      # toolTipProcDate=Datum der Prozedur
      # btnBack=Zur\u00FCck
      # lblDRG=DRG
      # lblPCCL=PCCL
      # lblMDC=MDC
      # btnHelp=Display help
      # hlpStay1=F\u00FCr den Gruppierungsvorgang ben\u00F6tigt der Grouper die Verweildauer des Patienten. Dies kann entweder direkt angegeben werden oder wird nach Eingabe von Aufnahme- und Entlassdatum sowie Urlaubstage vom System berechnet.
      # hlpStay2=Aufnahme- und Entlassart w\u00E4hlen Sie bitte aus den jeweiligen Listen.
      # hlpStay3=
      # hlpStay4=
      # hlpStay5=
      # hlpPatient1=In zahlreichen DRGs ist das Patientenalter gruppierungsrelevant. Sie k\u00F6nnen das Alter (zum Aufnahmezeitpunkt) entweder direkt angegeben, oder es durch Angabe von Aufnahmedatum und Geburtsdatum berechnen lassen. Das Alter wird in Jahren angegeben, sofern der Patient mindestens das erste Lebensjahr vollendet hat, andernfalls in Tagen. Die entsprechende Einheit ist aus der Liste zu w\u00E4hlen. 
      # hlpPatient2=F\u00FCr Patienten < 1 Jahr kann zus\u00E4tzlich das Aufnahmegewicht angegeben werden.
      # hlpPatient3=Das Geschlecht wird aus der Liste gew\u00E4hlt. 
      # hlpPatient4=Im Falle maschineller Beatmung wird diese ebenfalls erfasst, da sie f\u00FCr diverse DRGs gruppierungsrelevant ist.
      # hlpPatient5=
      # hlpPDX1=An dieser Stelle ist der Schl\u00FCssel der Hauptdiagnose anzugeben. Sofern es sich um einen korrekten Kode handelt, wird beim Serverzugriff die zugeh\u00F6rige Beschreibung geholt und angezeigt. Andernfalls erfolgt die Meldung eines unzul\u00E4ssigen Kodes.
      # hlpPDX2=
      # hlpPDX3=
      # hlpPDX4=
      # hlpPDX5=
      # hlpSDX1=Zu zahlreichen F\u00E4llen gibt es diverse Nebendiagnosen. Diese werden in der Tabelle erfasst.
      # hlpSDX2=Sofern der Eingabebereich zu klein ist, k\u00F6nnen Sie mittels Schaltfl\u00E4che [+] weitere Zeilen (max. zehn) einblenden.
      # hlpSDX3=\u00DCber die Schaltfl\u00E4che [-] kann die Anzahl der Zeilen wieder auf minimal zwei reduziert werden.
      # hlpSDX4=
      # hlpSDX5=
      # hlpProc1=Die Prozeduren werden in der Tabelle erfasst. Optional k\u00F6nnen Sie Seitenlokalisation und / oder Datum angeben.
      # hlpProc2=Mittels [+] und [-] kann die Anzahl der Eingabezeilen angepasst werden.
      # hlpProc3=
      # hlpProc4=
      # hlpProc5=
      # lblOther=Other
      # hlpOther1=Sind alle relevanten Falldaten erfasst, starten Sie \u00FCber [Gruppieren] die Auswertung. 
      # hlpOther2=Technically invalid entrys are highlighted in red (e.g. non-existent ICD-10 codes).
      # hlpOther6=All variables that are relevant for the grouping process are highlighted in green.
      # hlpOther3=Mit Hilfe der Schaltfl\u00E4che [Maske leeren] k\u00F6nnen Sie den Maskeninhalt entfernen, um so v\u00F6llig andere Falldaten zu erfassen. M\u00F6chten Sie dagegen \u00E4hnliche Daten gruppieren, so \u00E4ndern Sie einfach einzelne Werte und gruppieren neu.
      # hlpOther4=
      # hlpOther5=
      # lblSystem=System
      # lblGrouper=Grouper
      # lblCatProcs=Catalog procedures
      # lblCatDiags=Catalog diagnoses
      # hlpSystem1=Der Webgrouper unterst\u00FCtzt mehrere Systeme. W\u00E4hlen Sie die gew\u00FCnschte Grouperversion. Zu jedem System ist der passenden Prozeduren- sowie der Diagnosenkatalog hinterlegt. Diese werden nach Auswahl ebenfalls angezeigt.
      # hlpSystem2=
      # hlpSystem3=
      # hlpSystem4=
      # hlpSystem5=
      # lblResults=Grouping
      # lblGrouperStatus=Grouper status
      # lblFirstDaySurcharge=First day with surcharge
      # lblFirstDayDiscount=First day with discount
      # lblAvgDuration=Average length of stay
      # lblLowerGWD=Low trim point
      # lblUpperGWD=High trim point
      # lblAktLOS=Length of stay
      # lblCostWeight=Cost weight
      # lblBaseRate=Baserate
      # lblDiscountSurchargePerDay=Surcharge/discount rate per day
      # lblNumberOfExtraDays=Number of days relevant for surcharge/discount
      # lblEffectiveCostWeight=Effective cost weight
      # lblDiagnosis=Diagnosis
      # lblCode=Code
      # lblValid=Valid
      # lblUsed=Used
      # lblProcedures=Procedures / treatments
      # lblOrNor=OR/NOR
      # grpOK=Normal grouping
      # labelNoDiscountSurcharge=No surcharge / discount
      # labelDiscount=Discount rate per day
      # labelSurcharge=Surcharge rate per day
      # None=None
      # caseNormal=Inlier
      # caseUpperOutlier=High Outlier
      # caseLowerOutlier=Low Outlier
      # caseTransferred=Transferred
      # caseUnweighted=Unweighted DRG
      # diagDiagram=Chart for
      # diagLowerGVWD=LTP
      # diagUpperGVWD=HTP
      # diagAverageGVWD=Average LOS
      # diagCostWeight=Cost weight
      # diagLOS=Length of stay
      # labelTransferRatePerDay=Transfer discount rate per day
      # lblCCL=CCL
      # InvalidProcedure=Invalid procedure
      # InvalidDiagnosis=Invalid diagnosis
      # Valid=Valid
      # Invalid=Invalid
      # AlreadyInPDX=Already given as primary diagnosis
      # SexConflict=Sex conflict
      # AgeConflict=Age conflict
      # altOrNor=Invasive / Non-invasive procedure
      # altCCL=Complication and comorbidity level
      # searchDiags=Search diagnosis
      # searchProcs=Search procedures
      # diagActualVWD=actual LOS
      # acceptContract=Yes, I accept the terms of use.
      # lblHouses=Care provider
      # birthHouse=birth house
      # acuteInf=acute infirmary
      # settlementHints=settlement hints
      # additionalFees=additional fees: 
      # exceptionFromReuptake=Die DRG {0} ist von einer Fallzusammenführung bei Wiederaufnahme ausgeschlossen.
      
     